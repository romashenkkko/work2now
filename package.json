{
  "name": "work2now",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
